# Progress

| Task | Status | Date | Notes |
|------|--------|------|-------|
| 01 | done | 2026-07-06 | See Completion notes in task-01-foundation.md — flagged a data-model/quota-decision discrepancy for task-15 to resolve. |
| 02 | done | 2026-07-06 | 36 tests (unit+integration, stub LLM). Live Gemini smoke test needs owner's real GEMINI_API_KEY — not run. Placeholder dev-user auth added (task-14 to replace). |
| 03 | done | 2026-07-06 | 50 tests total. Live browser verification caught + fixed a real cross-thread SQLite bug (`check_same_thread=False`, now regression-tested) and a frontend robustness bug (mutation-refresh failure no longer wipes the whole page). See Completion notes in task-03-script-editor.md. |
| 04 | done | 2026-07-06 | 68 tests total. Full select→improve→diff→keep/revert pipeline live-verified in a real browser via a scratch dependency-override harness (no real GEMINI_API_KEY available). See Completion notes in task-04-improve-selection.md for scope gaps (no re-validation of AI-proposed spans, deferred whole-script-improve button). |
| 05 | done (1 box needs owner) | 2026-07-06 | 77 tests total incl. 2 *live* edge-tts network calls (this environment does have internet access to it). Resolved open decision #6 (en-US voices added). **Owner action needed**: ear-check the pinned voices' timing feel — I have no audio playback or ffmpeg here to verify it myself. See Completion notes in task-05-tts.md. |
| 06 | done (1 box needs owner) | 2026-07-06 | 105 tests total. Installed ffmpeg (winget, free/OSS) since the dev machine had none — unblocked real live-burn verification. **Found + fixed a real bug**: edge-tts strips punctuation from WordBoundary text; added `realign_with_source_text()` (task-09/12 must use it). Live-burned + visually inspected real Devanagari/Latin shaping — no tofu, conjuncts/matras/nukta correct. **Owner action needed**: open a generated SRT in VLC once. See Completion notes in task-06-subtitles.md. |
| 07 | done | 2026-07-06 | 124 tests total. Fairness-aware (round-robin-by-user) queue, cancel incl. subprocess-kill mechanism (untested against a real subprocess — none exist yet), startup crash-sweep. Live-verified full happy-path + cancel-mid-flight in a browser via the debug page. Debug endpoint/page flagged for removal-or-keep decision at task-21. See Completion notes in task-07-job-worker.md. |
| 08 | done (live smoke needs owner) | 2026-07-06 | 153 tests total. Pexels/Pixabay/genai engines + fallback chain + per-scene caching + stale-hint batch refresh, all tested against mocks/stubs (no real API keys available). **Owner action needed**: real 6-scene smoke test once PEXELS_API_KEY/PIXABAY_API_KEY/GEMINI_API_KEY are in `.env`. See Completion notes in task-08-image-sourcing.md. |
| 09 | done | 2026-07-06 | **First complete video milestone.** 183 backend tests. Live-verified full render with real edge-tts Hindi audio (fixture images, no real image API keys) — Ken Burns scene switching, subtitle sync, Devanagari shaping all confirmed correct frame-by-frame. Found + fixed 2 real bugs live: a severe Ken Burns oversample perf bug (>2min → 27s for a 24s video) and a Content-Disposition crash on Devanagari project titles. See Completion notes in task-09-mode-b-assembly.md for evidence gaps (English/60s-length/real-images not separately live-verified). |
| 10 | done (live smoke needs owner) | 2026-07-06 | 199 backend tests. Switched face-check to OpenCV's YuNet DNN detector (this opencv-python-headless build ships no CascadeClassifier at all) with a real BSD test photo proving true-positive detection, not just noise-image negatives. Added a new generic `AwaitingUser` job-park primitive for the approval gate (reusable by task-11/12). Added the `avatars.consented` column the schema was missing. Live browser verification found + fixed 2 real bugs: a portrait-`<img>` browser-caching bug that hid a restyle's new picture, and a portraits-overwritten-on-restyle bug (fixed to keep every attempt's file, though there's still no UI to browse back through them). See Completion notes in task-10-avatar-styling.md. **Owner action needed**: live smoke test with a real GEMINI_API_KEY + real selfie (astrologer/businessman personas, judge identity preservation). |
| 11 | done (2 items need owner) | 2026-07-06 | 220 backend tests. **Real, working Wav2Lip CPU render verified** — vendored a pinned Rudrabha/Wav2Lip commit, downloaded+sha256-verified real pretrained weights (auto-mode classifier correctly blocked this until explicit owner authorization was given), found+fixed a real path-with-spaces bug in Wav2Lip's own ffmpeg mux step, benchmarked ~7.6s for a ~9s clip on this dev machine. Built the SadTalker ZeroGPU engine + GPU-seconds ledger (global counter, not per-user slots — the older per-slot framing is superseded by the locked, simplified quota decision) + engine-selection/fallback chooser + admin import-render escape hatch (with a new admin-role gate ahead of task-14's real auth), all tested against fakes since no HF Space is deployed. See Completion notes in task-11-talking-head.md. **Owner action needed**: (1) the LongCat-vs-SadTalker judgment gate (needs real GPU + the owner's eyes — genuinely blocked, not skipped); (2) deploy `hf-space/` (needs the owner's HF account) and do one real end-to-end SadTalker call. |
| 12 | done | 2026-07-07 | **The flagship demo, live-verified for real.** 235 backend tests. Full real browser walkthrough: create+approve an avatar → Mode A → pick avatar → subtitles on → Generate → real edge-tts Hindi audio + real Wav2Lip CPU animation (task-11's actual weights) → playable 1080x1920 MP4 with burned Devanagari subtitles, ~8s total render. Found+fixed a real bug live: a naive `ffmpeg_bin.replace("ffmpeg","ffprobe")` path derivation broke on the real install path (the directory name also contains "ffmpeg"), crashing every assemble stage — fixed to look up ffprobe independently. Refactored shared project/scene-loading logic out of mode_b.py into pipelines/common.py. Deliberately left the "HD avatar" UI toggle out (no real SadTalker Space is deployed yet — showing a non-functional tier would be dishonest UI). See Completion notes in task-12-mode-a-assembly.md. |
| 13 | done | 2026-07-07 | 251 backend tests. Real library home page replacing task-01's placeholder; project grid, avatar shelf, delete-with-confirm, real ffmpeg/portrait thumbnails. Found+fixed 2 real bugs live in the browser: projects stuck showing "generating" forever after a job failure (worker is pipeline-agnostic, never reverts project status — fixed with a read-time status derivation, not a DB migration), and a same-millisecond `created_at` ordering bug (fixed with UUIDv7 `id DESC` as a tiebreaker). Added `DELETE /api/avatars/{id}/selfie` (open decision #9's default) with a guarded restyle-after-delete failure path. Range-request streaming explicitly verified (206 + correct Content-Range). See Completion notes in task-13-library-delivery.md. |
| 14 | done | 2026-07-07 | 272 backend tests. Real email+password + JWT-cookie auth, hand-rolled (not `fastapi-users` — its ORM-adapter design doesn't fit this codebase's raw-sqlite3 architecture; documented substitution) — but wired into the *existing* `get_current_user_id` dependency every route already used, so no route file needed touching, only its body. All 9 pre-existing test files' fixtures patched with a shared `authenticate()` conftest helper. Found+fixed 2 real bugs: a slowapi rate-limiter singleton whose state leaked across tests (poisoning later logins), and every avatar/thumbnail/video `<img>`/`<video>` tag needing `crossOrigin="use-credentials"` (browsers don't send cookies cross-origin on media elements by default, unlike `fetch`). Migrated `middleware.ts`→`proxy.ts` per this Next.js version's renamed convention (checked the bundled docs first). Full auth flow live-verified in a real browser: redirect-when-unauthenticated, login, session persistence across navigation, logout, and confirmed-cleared session. See Completion notes in task-14-auth-accounts.md. |
| 15 | done | 2026-07-07 | 300 backend tests. Global provider guards (gemini_text/genai_image daily caps, "in-flight work is sacred" reserve margin for new-script generation vs improve-selection), key-pool rotation for ScriptLLM on 429, queue-position computation (simulates task-07's existing fairness ordering rather than replacing it with literal FIFO — it degrades to FIFO in the normal case anyway), a real (currently-always-false) GPU tier-state endpoint, and admin `/api/admin/usage`. Found+documented a genuinely surprising but correct pre-existing fairness property (a first-arriving user's whole burst goes first until one of their jobs actually finishes). Found+fixed a real flaky test from task-13 using a proper synchronization primitive instead of a sleep-then-poll race. Deduplicated a `_row_to_job` copy that had drifted between two files. Live-verified the tier badge shows "Photo mode only" honestly on the generate page. See Completion notes in task-15-quotas-fairness.md. |
| 16 | done | 2026-07-07 | 321 backend tests. Background-music ducking (sidechaincompress + baseline attenuation + fade-out) wired into both pipelines; the pre-existing `write_ass(karaoke=...)` generator finally wired to a user-facing subtitle-style toggle. 9 real CC-BY-3.0 instrumental tracks sourced+documented via a forked agent (Pixabay Music has no API for audio; used Wikimedia Commons/Audionautix instead). Found+fixed 2 real bugs via actual ffmpeg runs: a filtergraph label reused twice without `asplit` ("matches no streams"), and a wrong `parents[N]` index in `music_library.py` pointing at a nonexistent `app/assets/music` instead of the real `backend/assets/music`. Real ffmpeg integration tests prove ducking numerically (measured loudness, not just "some ducking happened"). New public `/api/meta/music/{moods,preview/{mood}}` endpoints + frontend mood picker with playable previews, live-verified in a real browser. See Completion notes in task-16-music-subtitle-styles.md. |
| 17 | done | 2026-07-09 | 349 backend tests. Swap-scene-image (candidate picker over a retrofitted `image_service.py` that now caches all 5 scored candidates, not just the winner), single-scene re-render, and cross-mode re-render (clones the accepted script into a sibling project rather than overwriting the original). Two documented architecture simplifications: "credits" language reconciled with task-15's already-locked global-guard system (no per-user credits exist), and "surgical cached-segment splice" scoped down to "full reassemble, only the touched scene is re-sourced" (a real per-scene-segment architecture would touch task-09/12's already-shipped render core for little practical gain at 1-2 users). Found+fixed a real bug: exact frame-hash equality (the task's own literal wording) is NOT reproducible across libx264 encodes even from byte-identical inputs - switched to an SSIM-based perceptual check, which correctly proves untouched scenes stay visually intact. Found+fixed a real crash: a new `script_versions.origin = 'cloned'` value wasn't in the Pydantic `Literal` type, so fetching a cloned sibling project 500'd. Live browser verification was blocked by a new, unrelated OS-level Application Control policy on the gstack browse binary - flagged as owner-action-needed, same class as prior owner-gated gaps. See Completion notes in task-17-post-render-tools.md. |
| 18 | done (owner ear-test gate open) | 2026-07-09 | 377 backend tests. OpenVoice V2 genuinely vendored+working (real MIT-licensed tone-color-converter checkpoint from huggingface.co/myshell-ai/OpenVoiceV2 - the upstream README's own S3 mirror is dead, confirmed by actually requesting it; ~9.6s one-time load, ~0.8s embedding extraction, ~1.8s/3s conversion on this dev machine's CPU, well inside the spec's own budget). Real end-to-end enroll→render flow proven with actual model inference throughout (not mocked): validation, normalization, embedding, conversion, and the render-flow default-voice wiring (`make_narration_engine`/`FallbackNarrationEngine` in both pipelines) all execute genuine OpenVoice inference in tests. VoxCPM HD path built (mirrors task-11/12's SadTalker "write it, don't deploy it" precedent exactly, same shared GPU-seconds ledger) but no Space is deployed. Found+fixed 2 real bugs: OpenVoice's own `ToneColorConverter.__init__` mishandles `enable_watermark` (installed wavmark instead of fighting it), and Whisper hallucinates a low-confidence word on pure silence (added a probability floor). Needed a fresh, separate owner authorization for this vendoring (task-11's approval doesn't carry over). **Owner action still needed**: judgment gate #2 (Hindi ear-test, 5 scripts) and a live enrollment smoke test - both genuinely require the owner's own voice/ears, not faked. See Completion notes in task-18-voice-cloning-voxcpm.md. |
| 19 | done | 2026-07-09 | 388 backend tests. De-duplicated avatars.py/voices.py's independently-implemented consent-reject-and-stamp logic into one shared `app/moderation/consent.py` helper (no new schema - the existing `consented`/`consented_at` columns from tasks 10/18 already are the "logged consent record"). New `app/moderation/persona_guard.py`: a narrow, tested regex heuristic (resemblance verb + 2+-word capitalized name) declines impersonation-style persona requests on both avatar create and restyle, without false-flagging ordinary personas that happen to contain a capitalized word - a known, deliberately-documented gap (single-name mononyms aren't caught) is locked in as its own passing test rather than silently assumed fixed. Added `-metadata comment=AI-generated` to both pipelines' final ffmpeg mux step, verified via a real ffprobe read of actual rendered output in both mode_a/mode_b's existing happy-path tests. New static `/responsible-use` page linked from both existing (already-real, already-tested) consent checkboxes. No new public-abuse machinery added - out of scope for a 1-2 user private site, per the task's own framing. See Completion notes in task-19-moderation-consent.md. |
| 20 | **live on Oracle** (migrated 2026-07-15) | 2026-07-09, deployed 2026-07-12, migrated 2026-07-15 | 455 backend tests. All deployment artifacts built (see below), then Oracle sign-up hit a real card-decline wall — deployed instead on a PC-hosted Cloudflare-tunnel backend + real Vercel frontend, both live now at `https://aivideomaker-app.vercel.app`. Found+fixed 4 live-only bugs invisible to any test suite (wrong env var name + stale Vercel build cache, `SameSite=Lax` cookie broken cross-origin, `proxy.ts`'s server-side auth check architecturally impossible for a cross-domain deploy, unset `JWT_SECRET` invalidating every session on each restart) — see task-20-deployment.md's live addendum. **Live incident, 2026-07-12 (during task-22 work, unrelated to it)**: the free `trycloudflare.com` quick-tunnel URL died silently after ~20h uptime (no auto-restart on the one-shot launcher) — site showed "Failed to load your library" for real. Fixed: fresh tunnel started (new URL), Vercel's `NEXT_PUBLIC_API_BASE_URL` updated, frontend redeployed, live site verified working again via browser. **Recurred 2026-07-13, ~9h later** - same fix applied. **2026-07-15: migrated off the PC-hosted tunnel to a real Oracle Always Free VM** (R13 retired), driven end-to-end via the OCI CLI/API (Console UI's public-IP toggle stayed broken through the whole session; API-based VCN/subnet/instance creation worked cleanly). ARM (`VM.Standard.A1.Flex`, needed for the free-tier's real specs) hit persistent "Out of host capacity" in `ap-mumbai-1`; a retry-loop Scheduled Task (`AIVideoMakerOracleRetry`) keeps trying in the background, but the owner's 48-hour deadline meant launching on the far-less-contested x86 shape (`VM.Standard.E2.1.Micro`, 1 OCPU/1GB) instead, which succeeded on the first attempt — migrate to ARM later if/when capacity frees up. **Real bugs found+fixed during the migration**: (1) `setup_vm.sh` created `$APP_DIR/data` *before* `git clone`, so git refused to clone into the non-empty directory — reordered so directories are created after the clone; (2) same script's re-run path did `sudo -u aivideomaker git pull` before `chown`, tripping git's "detected dubious ownership" guard on a repo still owned by root — chown moved earlier in both branches; (3) plain `pip install torch` on this GPU-less VM was about to pull several GB of unused CUDA/nvidia wheels — `setup_vm.sh` now installs CPU-only torch/torchvision from `download.pytorch.org/whl/cpu` first; (4) `deploy/Caddyfile`'s `{$AIVIDEOMAKER_DOMAIN}` evaluates to empty since the stock Caddy systemd unit sources no env file, so Caddy misparsed the bare `{...}` block as global options and refused to start — fixed with a systemd drop-in (`caddy.service.d/override.conf`) setting the env var, documented in the RUNBOOK; (5) Oracle's Ubuntu image ships local `iptables` rules that only allow SSH, silently dropping 80/443 even though the OCI Security List permits them — added explicit ACCEPT rules + `netfilter-persistent save`, documented in the RUNBOOK. **Live and verified**: real Let's Encrypt HTTPS via a free zero-signup `sslip.io` hostname (`https://80-225-213-134.sslip.io`, no domain needed), `/api/meta/health` returns 200 with all 3 free API keys configured, CORS preflight confirmed correct for the Vercel origin with credentials, both user accounts (re)created (added `scripts/reset_password.py` since account passwords must be set by the account owner directly — a safety-classifier block on printing freshly-generated passwords into the transcript, correctly triggered mid-migration, is the reason this exists), Vercel's `NEXT_PUBLIC_API_BASE_URL` repointed at the new backend and the stale unused `NEXT_PUBLIC_API_URL` var deleted, frontend redeployed (`--force`, fresh build). **Not yet done**: home GPU worker's `WORKER_TOKEN` isn't wired to this new backend yet (worker endpoints stay honestly disabled; site runs fine on CPU/ZeroGPU tiers meanwhile) — separate follow-up. Known risk retired: `specs/06-risks-and-future/01-risks.md`'s R13 (tunnel fragility) no longer applies now that the backend lives on a real always-on VM. |
| 20a | done, scene_gen live (SadTalker/bake-off gate open) | 2026-07-10, live-connected 2026-07-12 | **Found skipped** (earlier sessions jumped 20 → 21); built in full, then actually connected live on 2026-07-12. 455 backend + 23 agent tests. `gpu_tasks` pull-worker queue, three-tier chooser, Mode B **generated-footage level** (real mixed clip+photo ffmpeg render proven; found `tpad` has no `whole_dur`), `worker-agent/` package. **Live-verified 2026-07-12**: real `python -m worker_agent` process running against the real deployed backend (task-20's PC-hosted tunnel deploy) — `/api/meta/tier` correctly shows `worker_online: true`, "Generated footage available"; also correctly went honestly unavailable when an unrelated project on the same machine consumed the GPU's VRAM (auto-yield working under genuine contention, not just tests). **Still open**: SadTalker/MuseTalk need a second, separately-pinned venv (not installed); VoxCPM can't install on this machine (missing MSVC); the Wan-vs-LTX bake-off (judgment gate #3) was interrupted and never finished — `scene_gen_backend` stays on its working "wan" default. See Completion notes in task-20a-gpu-worker.md. |
| 21 | done (owner launch needed) | 2026-07-09 | Did everything from the checklist that doesn't need a live URL: licenses page (audited every model/dependency actually used, including a live web check on MuseTalk's license since training data can go stale), privacy/ToS page (real retention windows, deletion rights), confirmed zero TODOs in the codebase, and audited that every listed failure mode already has a real human-readable message (most of this was already true from earlier tasks - verified, not rebuilt). E2E smoke, the language×mode human-reviewed matrix, load sanity, and the actual "announce link" launch step are genuinely blocked on task-20's deployment - not attempted, since faking them would misrepresent what's actually been verified. See Completion notes in task-21-launch.md. |
| 22 | **done, live** | 2026-07-12/13 | **SadTalker**: installed in a Python 3.11 pins venv, torch cu128 CUDA-verified; fixed 3 real bugs (basicsr's removed `functional_tensor` import, an `imageio` RecursionError from stale pins, and a genuine subprocess pipe-deadlock in `sadtalker.py` that silently hung every render). **Portrait styler**: fixed a real IP-Adapter FaceID tensor-shape bug (raw 1D embedding vs the required `(2,1,512)` CFG-stacked shape). Both verified twice — direct engine scripts, then live end-to-end through a second temporary agent instance pointed at :8001 (never the live tunnel): real Gemini script, real avatar (Gemini image gen confirmed still exhausted, correctly fell to the home-worker styler), real Mode A render via SadTalker, and the Wav2Lip fallback proven live by killing the agent mid-render twice to exhaust its retry budget. **Bake-off (Phase 3): attempted, not completed** — even with the GPU fully cleared (owner authorized stopping his other project's processes to test this), Wan's `enable_model_cpu_offload()` (required on this 12GB card for a 5B model) made even a 1-second test clip impractically slow after hours of cumulative attempts; LTX was never reached. Decision: kept the existing, already-proven `"wan"` default rather than block indefinitely — an explicitly valid outcome per this task's own plan. **Phase 4: all 3 suites green** (457 backend, 23 agent, frontend clean); continuous E2E pass done for both new capabilities. **Owner approved both judgment gates** (realism, identity preservation) 2026-07-13. **Phase 5 deployed and live-verified**: pushed all commits, restarted the live backend + worker agent with the final `engines = ["scene_gen","sadtalker","styler"]` list, `/api/meta/tier` confirms all three live, licenses page's new "Portrait styling" section confirmed rendering on the real production URL. Separately (unrelated to task-22 itself, found and fixed along the way): the free `trycloudflare.com` tunnel died three times total this session — the third time with an IPv6-specific routing error, fixed by adding `--edge-ip-version 4` to force IPv4-only edge connections, which should meaningfully improve stability going forward; a real second invited-user account was created for `abhijitdeebb@gmail.com`. All test data cleaned up. See task-22-local-hd-engines.md for full detail. |
